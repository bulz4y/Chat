import React from 'react';

import './ChatInfoType.css';

const ChatInfoType = (props) => {
    
    return (
        <div className="chat-info-type">
            <div className='chat-info-type__users active'>USERS</div>
        </div>
    );
    
};

export default ChatInfoType;